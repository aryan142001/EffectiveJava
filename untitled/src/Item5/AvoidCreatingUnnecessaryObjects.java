package Item5;

/**
 * The constrcutor must create a new object each time its called whereas
 * factory method is never required to do so
 *
 * For simple arithmetic operations like addition, subtraction, multiplication,
 * and division, using the primitive long type is preferred because it avoids the
 * overhead associated with object creation and method calls. It also provides better
 * performance since these operations can be directly performed by the CPU.
 */
public class AvoidCreatingUnnecessaryObjects {
}
