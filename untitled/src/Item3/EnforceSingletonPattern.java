package Item3;

/**
 * A singleton is simply a class that is instantiated exactly once
 *
 * Singleton usually represents a stateless object such as functions or a
 * system component that is intrinsically unique
 *
 * 2 common ways to implement singletons:-
 * A) Keeping the constructor private and exploring a public static member to provide
 * access to the sole instance
 *
 * B) In another method public member is a static factory method
 */

public class EnforceSingletonPattern {
}
